spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/avar
    username: postgres
    password: software
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
